rows = ["10", "100", "250"]
cols = ["model1", "conv", "attention", "conv and attention", "gaussian loss 1.0", "gaussian loss 0.5", "gaussian loss 0.1"]

for col in cols: 
    for row in rows:
        # get the correct image and append it to our grid

